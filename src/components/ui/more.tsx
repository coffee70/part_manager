import { Button } from './button'
import { MoreHorizontalIcon } from 'lucide-react'

export default function More() {
    return (
        <Button variant="icon">
            <MoreHorizontalIcon />
        </Button>
    )
}